server:
  port: 8080

spring:
  application:
    name: crowdfunding
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/crowdfunding-db
    username: root
    password: mika
  data:
    jdbc:
      dialect: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    locations: classpath:db/migration